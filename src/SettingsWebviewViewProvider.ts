import { WebviewView, Webview, Event, EventEmitter, WebviewViewProvider } from "vscode";

export class SettingsWebviewViewProvider implements WebviewViewProvider {
    resolveWebviewView(webviewView: WebviewView): void {
        // Implementação necessária para a interface WebviewViewProvider
    }
}
